\chapter{Parte 1}

\section{Exercício 1}

Considere uma lei de conservação genérica dada por

\[ \pd{u}{t} + \pd{f}{x} = 0 \]

Aproximamos $u$ e $f$ por $u_h$ e $f_h$, o que resulta em um Resíduo $R$ dado por

\[ R = \pd{u_h}{t} + \pd{f_h}{x} \]

Usando a técnica dos resíduos ponderados, temos que em cada elemento $k$

\[ \int_{D^k} R \cdot v \, dx = 0 \]

Fazendo $v$ como a função de Lagrange e substituindo a expressão de $R$, temos 

\[ \int_{D^k} \left( \pd{u_h}{t} + \pd{f_h}{x} \right)  L_j \, dx = 0 \]

\[ \int_{D^k} \pd{u_h}{t} L_j  \, dx + \int_{D^k} \pd{f_h}{x} L_j \, dx = 0 \]

Aplica integral por partes na segunda parcela da soma

\[ \int_{D^k} \pd{u_h}{t} L_j  \, dx = \int_{D^k} \od{L_j}{x} f \, dx - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

\textcolor{red}{Justificar segunda integral por partes, completar}

\section{Exercício 2}

Dentro de um elemento $D^k$, aproximamos $u(x,t)$ por uma interpolação 
por polinômios de Lagrange (nodal)

\[ u(x,t) = u_h(x,t) = \sum_{i=1}^{N_p} u_i(t) L_i(x) \]

Logo, 

\[ \pd{u_h}{t} = \sum_{i=1}^{N_p} \od{u_i}{t} L_i \]

Substitui isso na forma fraca

\[ \int_{D^k} \sum_{i=1}^{N_p} \od{u_i}{t} L_i L_j  \, dx = \int_{D^k} \od{L_j}{x} f \, dx - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

\[ \sum_{i=1}^{N_p} \od{u_i}{t} \int_{D^k} L_i L_j  \, dx = \int_{D^k} \od{L_j}{x} f \, dx - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

Definindo

\[ M_{ij} = \int_{D^k} L_i L_j  \, dx \]

Temos

\[ M \od{u}{t} = \dots \]

Agora analisamos a segunda integral da forma fraca. Substituindo $f = au$, temos 

\[ \int_{D^k} \od{L_j}{x} a u \, dx  \]

Substituindo $u_h$, 

\[ \int_{D^k} \od{L_j}{x} a \sum_{i=1}^{N_p} u_i(t) L_i(x) \, dx  \]

\[ a  \sum_{i=1}^{N_p} u_i(t) \int_{D^k} \od{L_j}{x}  L_i(x) \, dx  \]

Definindo

\[ S_{ij} = \int_{D^k} \od{L_j}{x}  L_i(x) \, dx \]

Temos

\[  M \od{\mathbf{u}}{t} = a S \mathbf{u} - \mathbf{F} \]

Usar a representação nodal é mais conveniente do ponto de vista de código 
uma vez que armazenamos os pontos (nós) em que a função $u_h$ é avaliada.

\section{Exercício 3}

Quando $\alpha = 1$, a segunda parcela se anula e temos apenas a média aritmética das bordas
de ambos
elementos vizinhos.

Quando $\alpha = 0$, o fluxo leva em consideração a direção de propagação por meio 
dos termos $\hat{n}^+$ e $\hat{n}^-$.

\section{Exercício 4}

Os pontos GLL são escolhidos ao invés dos pontos equidistantes pois 
ele apresentam melhor desempenho no método numérico. 
Além disso, a matriz M se torna aproximadamente diagonal ao escolher
os pontos GLL, o que melhora o desempenho computacional.

Os pontos GLL são tabelados, tornando fácil seu uso.

\section{Exercício 5}

O método RK4 é alta ordem, sendo natural seu uso com o DGTD que também é de alta
ordem. Usar o método de Euler após o obter a forma semi-discreta iria jogar fora 
todo o ganho de acurácia que obtemos com o DGTD.

Na forma semi-discreta, temos a forma 

\[  M \od{\mathbf{u}}{t} = a S \mathbf{u} - \mathbf{F} \]

\[  \od{\mathbf{u}}{t} = (M)^{-1} a S \mathbf{u} - \mathbf{F} \]

\[  \od{\mathbf{u}}{t} = R(\mathbf{u}) \]

\noindent que pode ser resolvido através de 

\[
\frac{d\mathbf{u}}{dt} = R(\mathbf{u})
\]

\[
\mathbf{k}_1 = R(\mathbf{u}^n)
\]

\[
\mathbf{k}_2 = R\left(\mathbf{u}^n + \frac{\Delta t}{2}\mathbf{k}_1\right)
\]

\[
\mathbf{k}_3 = R\left(\mathbf{u}^n + \frac{\Delta t}{2}\mathbf{k}_2\right)
\]

\[
\mathbf{k}_4 = R\left(\mathbf{u}^n + \Delta t\,\mathbf{k}_3\right)
\]

\[
\mathbf{u}^{n+1}
=
\mathbf{u}^n
+
\frac{\Delta t}{6}
\left(
\mathbf{k}_1
+
2\mathbf{k}_2
+
2\mathbf{k}_3
+
\mathbf{k}_4
\right)
\]

\textcolor{red}{Mostra pq usa low storage RK4 em vez do RK4 convencional, qual
a diferença}

\section{Exercício 6}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex1-6-codigo}.

Usando o método do DGTD para a equação da advecção 1D e os seguintes parâmetros de 
discretização:

\begin{itemize}
	\item $K = 64$
    \item $N = 4$
    \item $\Delta t = 1.25 \cdot 10^{-4} \un{s}$ (CFL = 0.2)
	\item $\Delta x = 1.5 \cdot 10^{-2} \un{m}$
	\item $T = 1 \un{s} \quad \Longrightarrow \quad N_t = 8000$
\end{itemize}

\noindent podemos simular um período completo de uma advecção 1D
com velocidade $a = 1$. O resultado obtido encontra-se na Figura \ref{fig:ex1-6-general}.
Vemos que o sinal se propaga para a direita e retorna para a posição inicial após um 
período completo, com a condição de contorno períodica.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-6-general}Propagação da advecção 1D ao longo do tempo.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-6-general.png}
	\end{center}
\end{figure}

Monitorando a posição valor máximo do ponto ao longo do tempo, podemos traçar sua 
posição ao longo do tempo e assim calcular a velocidade de propagação numérica 
para os fluxos central e upwind exibido na Figura \ref{fig:ex1-6-velocidade}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-6-velocidade}Posição do pico do sinal ao longo do tempo para ambos os fluxos.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-6-velocidade.png}
	\end{center}
\end{figure}

Em ambos casos, a inclinação da reta na Figura \ref{fig:ex1-6-velocidade} nos retorna a velocidade de propagação 
numérica e, a partir de uma regressão linear simples, obtemos $a_{NUM} = 1.009 \un{m/s}$, com um 
erro de apenas 0.9 \% em relação ao valor analítico, sendo esse um indicador do funcionamento 
da implementação.

Uma outra maneira de testar a implementação é comparar com a solução analítica, dada por

\[ u_{exact}(x,t) = u_0(x - at) = \sin\left(2\pi (x - t)\right)  \]

\noindent através da norma-$L^2$ e analisar a convergência conforme aumentamos o número de 
elementos $K$ no domínio. O resultado da convergência está exibido na Figura 
\ref{fig:ex1-6-convergencia}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-6-convergencia}Convergência da norma-L2 conforme aumenta-se o número de elementos $K$.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-6-convergencia.png}
	\end{center}
\end{figure}

Era esperado que a norma se reduzisse por um fator mínimo de 2 a cada vez que $K$ dobra.
No entanto, isso não é observada na Figura \ref{fig:ex1-6-convergencia}, indicando que 
há possibilidades de melhorar o código.

\section{Exercício 7}

Usando a mesma discretização do exercício 6, mudando apenas a condição inicial para um pulso
gaussiano no centro do domínio, obtemos a propagação ao longo do tempo exibida na 
Figura \ref{fig:ex1-7-propagacao-direita} para $a = + 1 \un{m/s}$. Invertendo o sinal da velocidade, 
temos que o sinal se propaga para a esquerda com $a = - 1 \un{m/s}$ conforme mostra 
a Figura \ref{fig:ex1-7-propagacao-esquerda}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-7-propagacao-direita}Propagação do pulso gaussiano para $a = + 1 \un{m/s}$.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-7-propagacao-direita.png}
	\end{center}
\end{figure}

\begin{figure}[htbp]
	\caption{\label{fig:ex1-7-propagacao-esquerda}Propagação do pulso gaussiano para $a = - 1 \un{m/s}$.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-7-propagacao-esquerda.png}
	\end{center}
\end{figure}

Usando a mesma técnica do exercício 6 de monitorar a posição do pico do sinal ao longo 
do tempo e plotar a curva posição x tempo, tomando a regrressão linear para calcular a velocidade, 
obtemos a velocidade numérica (para o pulso se deslocando para a esquerda) de $a_{NUM} = - 1.004 \un{m/s}$, 
com um erro de 0.4 \% em relação ao valor teórico, como mostra a Figura \ref{fig:ex1-7-velocidade}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-7-velocidade}Posição do pico do sinal ao longo do tempo.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-7-velocidade.png}
	\end{center}
\end{figure}

\section{Exercício 8}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex1-8-codigo}.

Usando o método do DGTD para a equação da advecção 1D e os seguintes parâmetros de 
discretização:

\begin{itemize}
	\item $K = 64$
    \item $N = 4$
    \item $\Delta t = 2.5 \cdot 10^{-4} \un{s}$ (CFL = 0.2)
	\item $\Delta x = 1.96 \cdot 10^{-1} \un{m}$
	\item $T = 10 \un{s} \quad \Longrightarrow \quad N_t = 40000$
\end{itemize}

\noindent obtemos a seguinte propagação da onda para a direita a partir da condição inicial,
ao aplicar a fonte senoidal na extremidade esquerda,
exibida na Figura \ref{fig:ex1-8-propagacao}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-8-propagacao}Propagação da onda para a direita ao longo do tempo.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-8-propagacao.png}
	\end{center}
\end{figure}

A solução analítica é dada por

\[ u_{exact}(x,t=10\un{s}) = \sin \left(x\right)  \]

\noindent que está sobreposta à solução numérica obtida como mostra a Figura 
\ref{fig:ex1-8-solucao}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-8-solucao}Solução em t = 10 s.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-8-solucao.png}
	\end{center}
\end{figure}

A norma-$L^2$ calculada entre as soluções numérica e analítica, para diferentes valores de $K$,
está exibida na Tabela \ref{tab:ex1-8-convergencia}. Vemos que o erro se reduz conforme aumentamos
o número de elementos $K$ da malha.

\begin{table}[h!]
	\caption{Norma-L2 em t = 10 s para diferentes valores de $K$.}
	\vspace{0.5cm}
	\centering
		\begin{tabular}{c|c}

		$K$ & Norma-$L^2$ \\ \hline
		$8$    & $0.904$      \\ \hline
		$16$    & $0.673$    \\ \hline
		$32$    & $0.485$       \\ \hline
		$64$    & $0.34$     \\
		\end{tabular}
	\label{tab:ex1-8-convergencia}
\end{table}