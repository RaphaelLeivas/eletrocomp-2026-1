\chapter{Parte 1}

\section{Exercício 1}

Considere uma lei de conservação genérica dada por

\[ \pd{u}{t} + \pd{f}{x} = 0 \]

Aproximamos $u$ e $f$ por $u_h$ e $f_h$, o que resulta em um Resíduo $R$ dado por

\[ R = \pd{u_h}{t} + \pd{f_h}{x} \]

Usando a técnica dos resíduos ponderados, temos que em cada elemento $k$

\[ \int_{D^k} R \cdot v \, dx = 0 \]

Fazendo $v$ como a função de Lagrange e substituindo a expressão de $R$, temos 

\[ \int_{D^k} \left( \pd{u_h}{t} + \pd{f_h}{x} \right)  L_j \, dx = 0 \]

\[ \int_{D^k} \pd{u_h}{t} L_j  \, dx + \int_{D^k} \pd{f_h}{x} L_j \, dx = 0 \]

Aplica integral por partes na segunda parcela da soma

\[ \int_{D^k} \pd{u_h}{t} L_j  \, dx = \int_{D^k} \od{L_j}{x} f \, dx - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

Obtemos a forma fraca do esquema. Aplicando integração por partes uma segunda vez, obtemos
a forma forte:

\[ \int_{D^k} \pd{u_h}{t} L_j  \, dx = \left[ L_j f \right]_{x_L}^{x_R} - \int_{D^k} \od{f}{x} L_j  \, dx   - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

\[ \int_{D^k} \pd{u_h}{t} L_j  \, dx + \int_{D^k} \pd{f}{x} L_j + \left[ L_j (f^* - f) \right]_{x_L}^{x_R} = 0 \]




\section{Exercício 2}

Dentro de um elemento $D^k$, aproximamos $u(x,t)$ por uma interpolação 
por polinômios de Lagrange (nodal)

\[ u(x,t) = u_h(x,t) = \sum_{i=1}^{N_p} u_i(t) L_i(x) \]

Logo, 

\[ \pd{u_h}{t} = \sum_{i=1}^{N_p} \od{u_i}{t} L_i \]

Substitui isso na forma fraca

\[ \int_{D^k} \sum_{i=1}^{N_p} \od{u_i}{t} L_i L_j  \, dx = \int_{D^k} \od{L_j}{x} f \, dx - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

\[ \sum_{i=1}^{N_p} \od{u_i}{t} \int_{D^k} L_i L_j  \, dx = \int_{D^k} \od{L_j}{x} f \, dx - \left[ L_j f \right]_{x_L}^{x_R}  = 0 \]

Definindo

\[ M_{ij} = \int_{D^k} L_i L_j  \, dx \]

Temos

\[ M \od{u}{t} = \dots \]

Agora analisamos a segunda integral da forma fraca. Substituindo $f = au$, temos 

\[ \int_{D^k} \od{L_j}{x} a u \, dx  \]

Substituindo $u_h$, 

\[ \int_{D^k} \od{L_j}{x} a \sum_{i=1}^{N_p} u_i(t) L_i(x) \, dx  \]

\[ a  \sum_{i=1}^{N_p} u_i(t) \int_{D^k} \od{L_j}{x}  L_i(x) \, dx  \]

Definindo

\[ S_{ij} = \int_{D^k} \od{L_j}{x}  L_i(x) \, dx \]

Temos

\[  M \od{\mathbf{u}}{t} = a S \mathbf{u} - \mathbf{F} \]

Usar a representação nodal é mais conveniente do ponto de vista de código 
uma vez que armazenamos os pontos (nós) em que a função $u_h$ é avaliada.

\section{Exercício 3}

Quando $\alpha = 1$ (fluxo central), a segunda parcela se anula e temos apenas a média aritmética das bordas
de ambos
elementos vizinhos.

Quando $\alpha = 0$ (fluxo upwind), o fluxo leva em consideração a direção de propagação por meio 
dos termos $\hat{n}^+$ e $\hat{n}^-$.

\section{Exercício 4}

Os pontos GLL são escolhidos ao invés dos pontos equidistantes pois 
ele apresentam melhor desempenho no método numérico. 
Além disso, a matriz M se torna aproximadamente diagonal ao escolher
os pontos GLL, o que melhora o desempenho computacional.

Os pontos GLL são tabelados, tornando fácil seu uso.

\section{Exercício 5}

Uma vez obtido a forma semi-discreta com o DGTD:

\[ \frac{d\mathbf{u}}{dt} = \mathbf{R}(\mathbf{u}) \]

\noindent usamos o método de
Runge-Kutta explícito de quarta ordem de baixo armazenamento
(Low-Storage Runge-Kutta -- LSRK4). Diferentemente do RK4 clássico,
esse algoritmo necessita armazenar apenas duas variáveis de trabalho,
reduzindo significativamente o consumo de memória.

Definindo $\mathbf{r}$ como o vetor residual e
$\Delta t$ como o passo de tempo, o algoritmo é dado por

\begin{align}
\mathbf{r}^{(i)} &=
a_i\,\mathbf{r}^{(i-1)}
+
\Delta t\,\mathbf{R}\!\left(\mathbf{u}^{(i-1)}\right),
\\
\mathbf{u}^{(i)}
&=
\mathbf{u}^{(i-1)}
+
b_i\,\mathbf{r}^{(i)},
\qquad i=1,\ldots,5,
\end{align}

onde $\mathbf{u}^{(0)}=\mathbf{u}^n$,
$\mathbf{u}^{(5)}=\mathbf{u}^{n+1}$ e
$\mathbf{r}^{(0)}=\mathbf{0}$.

E os coeficientes $a$ e $b$ são dados por 

\begin{align}
a &=
\left[
0,\,
-\frac{567301805773}{1357537059087},\,
-\frac{2404267990393}{2016746695238},\,
-\frac{3550918686646}{2091501179385},\,
-\frac{1275806237668}{842570457699}
\right],
\\
b &=
\left[
\frac{1432997174477}{9575080441755},\,
\frac{5161836677717}{13612068292357},\,
\frac{1720146321549}{2090206949498},\,
\frac{3134564353537}{4481467310338},\,
\frac{2277821191437}{14882151754819}
\right].
\end{align}

O algoritmo completo pode ser descrito pelo seguinte pseudocódigo:

\begin{verbatim}
r = 0
u = u^n

for i = 1,...,5
    r = a(i)*r + dt*R(u)
    u = u + b(i)*r
end

u^(n+1) = u
\end{verbatim}

Esse esquema preserva a precisão de quarta ordem no tempo,
apresentando custo computacional semelhante ao RK4 clássico, porém
com menor demanda de memória. Essa característica o torna
particularmente adequado para métodos de alta ordem, como o DGTD,
nos quais o número de graus de liberdade é elevado.

\section{Exercício 6}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex1-6-codigo}.

Usando o método do DGTD para a equação da advecção 1D e os seguintes parâmetros de 
discretização:

\begin{itemize}
	\item $K = 64$
    \item $N = 4$
    \item $\Delta t = 1.25 \cdot 10^{-4} \un{s}$ (CFL = 0.2)
	\item $\Delta x = 1.5 \cdot 10^{-2} \un{m}$
	\item $T = 1 \un{s} \quad \Longrightarrow \quad N_t = 8000$
\end{itemize}

\noindent podemos simular um período completo de uma advecção 1D
com velocidade $a = 1$. O resultado obtido encontra-se na Figura \ref{fig:ex1-6-general}.
Vemos que o sinal se propaga para a direita e retorna para a posição inicial após um 
período completo, com a condição de contorno períodica.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-6-general}Propagação da advecção 1D ao longo do tempo.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-6-general.png}
	\end{center}
\end{figure}

Monitorando a posição valor máximo do ponto ao longo do tempo, podemos traçar sua 
posição ao longo do tempo e assim calcular a velocidade de propagação numérica 
para os fluxos central e upwind exibido na Figura \ref{fig:ex1-6-velocidade}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-6-velocidade}Posição do pico do sinal ao longo do tempo para ambos os fluxos.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-6-velocidade.png}
	\end{center}
\end{figure}

Em ambos casos, a inclinação da reta na Figura \ref{fig:ex1-6-velocidade} nos retorna a velocidade de propagação 
numérica e, a partir de uma regressão linear simples, obtemos $a_{NUM} = 1.009 \un{m/s}$, com um 
erro de apenas 0.9 \% em relação ao valor analítico, sendo esse um indicador do funcionamento 
da implementação.

Uma outra maneira de testar a implementação é comparar com a solução analítica, dada por

\[ u_{exact}(x,t) = u_0(x - at) = \sin\left(2\pi (x - t)\right)  \]

\noindent através da norma-$L^2$ e analisar a convergência conforme aumentamos o número de 
elementos $K$ no domínio. O resultado da convergência está exibido na Figura 
\ref{fig:ex1-6-convergencia}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-6-convergencia}Convergência da norma-L2 conforme aumenta-se o número de elementos $K$.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-6-convergencia.png}
	\end{center}
\end{figure}

Era esperado que a norma se reduzisse por um fator mínimo de 2 a cada vez que $K$ dobra.
No entanto, isso não é observada na Figura \ref{fig:ex1-6-convergencia}, indicando que 
há possibilidades de melhorar o código.

\section{Exercício 7}

Usando a mesma discretização do exercício 6, mudando apenas a condição inicial para um pulso
gaussiano no centro do domínio, obtemos a propagação ao longo do tempo exibida na 
Figura \ref{fig:ex1-7-propagacao-direita} para $a = + 1 \un{m/s}$. Invertendo o sinal da velocidade, 
temos que o sinal se propaga para a esquerda com $a = - 1 \un{m/s}$ conforme mostra 
a Figura \ref{fig:ex1-7-propagacao-esquerda}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-7-propagacao-direita}Propagação do pulso gaussiano para $a = + 1 \un{m/s}$.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-7-propagacao-direita.png}
	\end{center}
\end{figure}

\begin{figure}[htbp]
	\caption{\label{fig:ex1-7-propagacao-esquerda}Propagação do pulso gaussiano para $a = - 1 \un{m/s}$.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-7-propagacao-esquerda.png}
	\end{center}
\end{figure}

Usando a mesma técnica do exercício 6 de monitorar a posição do pico do sinal ao longo 
do tempo e plotar a curva posição x tempo, tomando a regrressão linear para calcular a velocidade, 
obtemos a velocidade numérica (para o pulso se deslocando para a esquerda) de $a_{NUM} = - 1.004 \un{m/s}$, 
com um erro de 0.4 \% em relação ao valor teórico, como mostra a Figura \ref{fig:ex1-7-velocidade}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-7-velocidade}Posição do pico do sinal ao longo do tempo.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-7-velocidade.png}
	\end{center}
\end{figure}

\section{Exercício 8}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex1-8-codigo}.

Usando o método do DGTD para a equação da advecção 1D e os seguintes parâmetros de 
discretização:

\begin{itemize}
	\item $K = 64$
    \item $N = 4$
    \item $\Delta t = 2.5 \cdot 10^{-4} \un{s}$ (CFL = 0.2)
	\item $\Delta x = 1.96 \cdot 10^{-1} \un{m}$
	\item $T = 10 \un{s} \quad \Longrightarrow \quad N_t = 40000$
\end{itemize}

\noindent obtemos a seguinte propagação da onda para a direita a partir da condição inicial,
ao aplicar a fonte senoidal na extremidade esquerda,
exibida na Figura \ref{fig:ex1-8-propagacao}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-8-propagacao}Propagação da onda para a direita ao longo do tempo.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-8-propagacao.png}
	\end{center}
\end{figure}

A solução analítica é dada por

\[ u_{exact}(x,t=10\un{s}) = \sin \left(x\right)  \]

\noindent que está sobreposta à solução numérica obtida como mostra a Figura 
\ref{fig:ex1-8-solucao}.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-8-solucao}Solução em t = 10 s.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-8-solucao.png}
	\end{center}
\end{figure}

A norma-$L^2$ calculada entre as soluções numérica e analítica, para diferentes valores de $K$,
está exibida na Tabela \ref{tab:ex1-8-convergencia}. Vemos que o erro se reduz conforme aumentamos
o número de elementos $K$ da malha.

\begin{table}[h!]
	\caption{Norma-L2 em t = 10 s para diferentes valores de $K$.}
	\vspace{0.5cm}
	\centering
		\begin{tabular}{c|c}

		$K$ & Norma-$L^2$ \\ \hline
		$8$    & $0.904$      \\ \hline
		$16$    & $0.673$    \\ \hline
		$32$    & $0.485$       \\ \hline
		$64$    & $0.34$     \\
		\end{tabular}
	\label{tab:ex1-8-convergencia}
\end{table}

\section{Exercício 9}

O código usado nesse exercício é essencialmente o mesmo do exercício 6
presente no Anexo \ref{cap:ex1-6-codigo}, usando o seguinte comando para a condição
inicial do pulso retangular:

\begin{tiny}
	\begin{verbatim}
	u = np.where(np.abs(x_nodes - 0.3) <= (5 * dx)/2, 1.0, 0.0).flatten().copy()
	\end{verbatim}
\end{tiny}

O primeiro passo é definir a função degrau que será a condição inicial. 
A função usada está exibida na Figura \ref{fig:ex1-9-inicial}, 
com largura de $5 \cdot \Delta x$.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-9-inicial}Condição inicial.}
	\begin{center}
        \includegraphics[width=0.8\textwidth,trim=1 1 1 1,clip]{ex1-9-inicial.png}
	\end{center}
\end{figure}

Os parâmetros de discretização do domínio $\left[ 0 \, , \, 1 \right]$ adotados são:

\begin{itemize}
	\item $K = 50$
    \item $N = 4$
    \item $\Delta t = 8 \cdot 10^{-5} \un{s}$ (CFL = 0.2)
	\item $\Delta x = 2 \cdot 10^{-2} \un{m}$
	\item $T = 3 \un{s} \quad \Longrightarrow \quad N_t = 37500$
	\item $a = 2 \un{m/s}$
\end{itemize}

O resultado obtido encontra-se na Figura \ref{fig:ex1-9-propagacao} para o fluxo 
numérico upwind.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-9-propagacao}Propagação do pulso retangular ao longo do tempo - fluxo upwind.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-9-propagacao.png}
	\end{center}
\end{figure}

Com o fluxo central, temos um resultado diferente como mostra a Figura \ref{fig:ex1-9-propagacao-central}.
As descontinuidades introduzem oscilasções ao longo de todo o sinal, ao contrário do fluxo
central em que essas oscilasções ocorrem apenas ao redor das bordas do pulso.

\begin{figure}[htbp]
	\caption{\label{fig:ex1-9-propagacao-central}Propagação do pulso retangular ao longo do tempo - fluxo central.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex1-9-propagacao-central.png}
	\end{center}
\end{figure}

\chapter{Parte 2}

\section{Exercício 1}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex2-1-codigo}.

Usando o método do DGTD para as equações de Maxwell 1D com as seguintes discretizações:

\begin{itemize}
	\item $K = 64$
    \item $N = 4$
    \item $\Delta t = 1.25 \cdot 10^{-4} \un{s}$ (CFL = 0.2)
	\item $\Delta x = 1.56 \cdot 10^{-2} \un{m}$
	\item $T = 3 \un{s} \quad \Longrightarrow \quad N_t = 24000$
\end{itemize}

\noindent obtendo o resultado exibido na Figura \ref{fig:ex2-1-propagacao} para o fluxo central, 
e a Figura \ref{fig:ex2-1-propagacao-upwind} para o fluxo upwind. Vemos que o resultado obtido é o mesmo
para ambos os casos.

\begin{figure}[htbp]
	\caption{\label{fig:ex2-1-propagacao}Progação de $E_y$ e $H_z$ ao longo do tempo - fluxo central.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex2-1-propagacao.png}
	\end{center}
\end{figure}

\begin{figure}[htbp]
	\caption{\label{fig:ex2-1-propagacao-upwind}Progação de $E_y$ e $H_z$ ao longo do tempo - fluxo upwind.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex2-1-propagacao-upwind.png}
	\end{center}
\end{figure}

Sabemos de exercícios anteriores que a solução exata para esse problema é dada por

\[ E_y = \sin (\pi x) \cos (\pi t) \quad , \quad H_z = -\cos (\pi x) \sin (\pi t) \]

\noindent de modo que podemos tomar a norma-$L^2$ para verificar o desempenho do método,
como mostra a Tabela \ref{tab:ex2-1-convergencia}. Conforme esperado, o erro se reduz conforme
aumentamos o número de elementos $K$ da malha, contudo ainda não na velocidade esperada.
Como o método de ordem elevada, era esperado que a norma-$L^2$ do erro se reduzisse no mínimo
por um fator 4 quando aumentamos $K$ por um fator 2.

\begin{table}[h!]
	\caption{Norma-L2 para diferentes valores de $K$.}
	\vspace{0.5cm}
	\centering
		\begin{tabular}{c|c}

		$K$ & Norma-$L^2$ \\ \hline
		$16$    & $0.121$    \\ \hline
		$32$    & $0.063$       \\ \hline
		$64$    & $0.042$     \\
		\end{tabular}
	\label{tab:ex2-1-convergencia}
\end{table}

\section{Exercício 2}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex2-2-codigo}.

Com a mesma discretização do item anterior, agora propagamos um pulso gaussiano para a direita 
a fim de verificar a implementação da condição de contorno absorvente de SMC. O pulso
inicial usado é dado por 

\[ E_y(x) = \exp \left[ - \frac{1}{2} \frac{(x - 0.3)^2}{0.08} \right] \]

\noindent o que resulta na propagação exibida na Figura \ref{fig:ex2-2-propagacao} com o fluxo central.
Observa-se que não há nenhuma onda refletida a partir da borda direita após a passagem do sinal, 
indicando um bom funcionamento da condição de contorno.

\begin{figure}[htbp]
	\caption{\label{fig:ex2-2-propagacao}Propagação do pulso gaussiano para a direita em função do tempo - fluxo central.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex2-2-propagacao.png}
	\end{center}
\end{figure}

\section{Exercício 3}

Todo o código usado nesse exercício encontra-se no Anexo \ref{cap:ex2-3-codigo}.

A Figura \ref{fig:ex2-3-propagacao} mostra a propagação do campo $E_y$ com o pulso gaussiano
para a direita com condições absorventes SMC e com a barra dielétrica $\epsilon = 3$ em 
$0 \leq x \leq 0.5$.

\begin{figure}[htbp]
	\caption{\label{fig:ex2-3-propagacao}Propagação de $E_y$ ao longo do tempo com a barra dielétrica.}
	\begin{center}
        \includegraphics[width=\textwidth,trim=1 1 1 1,clip]{ex2-3-propagacao.png}
	\end{center}
\end{figure}

Observamos a presença de reflexões em $x = 0$ e $x = 0.5$, conforme esperado, uma vez que nesses
pontos temos descontinuidades de impedância devido à mudanças de meio, resultando em 
reflexões no sinal. 